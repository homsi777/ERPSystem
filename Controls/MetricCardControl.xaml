<UserControl x:Class="ERPSystem.Controls.MetricCardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border x:Name="CardBorder" Background="White"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
            CornerRadius="8" Padding="12,10"
            HorizontalAlignment="Stretch"
            Effect="{StaticResource CardShadow}"
            Cursor="Hand">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="6"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" x:Name="TxtTitle"
                           Style="{StaticResource CaptionTextStyle}"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           TextWrapping="Wrap"/>
                <Border Grid.Column="1" x:Name="IconBorder"
                        Width="36" Height="36" CornerRadius="8">
                    <TextBlock x:Name="TxtIcon"
                               FontFamily="Segoe MDL2 Assets" FontSize="16"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </Grid>

            <TextBlock Grid.Row="2" x:Name="TxtValue"
                       Style="{StaticResource KpiValueTextStyle}"/>

            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" x:Name="TrendBorder"
                        CornerRadius="100" Padding="6,2" Margin="0,0,6,0">
                    <TextBlock x:Name="TxtTrend" FontSize="10" FontWeight="SemiBold"
                               FontFamily="Segoe UI, Tahoma, Arial"/>
                </Border>
                <TextBlock Grid.Column="1" x:Name="TxtDescription"
                           Style="{StaticResource CaptionTextStyle}"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"/>
            </Grid>

            <Border x:Name="AccentBar" Width="0" Visibility="Collapsed"/>
        </Grid>
    </Border>
</UserControl>
