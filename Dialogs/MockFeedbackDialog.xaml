<Window x:Class="ERPSystem.Dialogs.MockFeedbackDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ERP PRO"
        Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft"
        Background="{StaticResource AppBgBrush}"
        FontFamily="Segoe UI, Tahoma, Arial">

    <Border Style="{StaticResource CardStyle}" Margin="20">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <Border x:Name="IconBadge" Width="40" Height="40" CornerRadius="8" Margin="0,0,12,0">
                    <TextBlock x:Name="TxtIcon" FontFamily="Segoe MDL2 Assets" FontSize="18"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock x:Name="TxtTitle" FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock x:Name="TxtSubtitle" FontSize="11"
                               Foreground="{StaticResource TextMutedBrush}" Margin="0,2,0,0"/>
                </StackPanel>
            </StackPanel>
            <TextBlock x:Name="TxtMessage" TextWrapping="Wrap" FontSize="13"
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,16"/>
            <Button x:Name="BtnOk" Content="حسناً" Style="{StaticResource PrimaryButtonStyle}"
                    HorizontalAlignment="Left" Width="100" Height="36"
                    IsDefault="True" Click="BtnOk_Click"/>
        </StackPanel>
    </Border>
</Window>
